name: VM4 R3 Head Freshness Delay
on:
  pull_request:
permissions:
  contents: read
jobs:
  delay:
    name: VM4 R3 Head Freshness Delay
    runs-on: ubuntu-latest
    steps:
      - run: sleep 60
      - run: echo VM4_R3_OK
