merge_queue:
  max_parallel_checks: 1

queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = VM4 auto-timeout hold
    merge_method: merge
    checks_timeout: auto

commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm4-secondary-20260911
